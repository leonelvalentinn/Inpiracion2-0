---
interface Props {
  classList?: string;
}

const { classList } = Astro.props;
---

<a
  class={`inline-block w-max h-max px-5 py-2 rounded-full text-white bg-violet-600 font-light transition hover:scale-105 ${classList}`}
  href="tel:555555555555"
  target="_blank"
  rel="noopener noreferrer"
  aria-label="Ir al WhatsApp de Inspiración Creativa"
>
  Contacto
</a>
